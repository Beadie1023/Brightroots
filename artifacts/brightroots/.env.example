# Absolute URL of the deployed API server (Render).
# Leave unset in local/Replit dev — the shared proxy handles /api routing.
# Example: https://brightroots-api.onrender.com
VITE_API_URL=https://onrender.com
